import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const Color background = Color(0xFF1C1C1E);
  static const Color cardBackground = Color(0xFF2C2C2E);
  static const Color projectCardBackground = Color(0xFF4A3B89);
  static const Color accent = Color(0xFF0066FF);

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0x80FFFFFF); // 50% opacity white
  static const Color textInactive = Color(0xFF666666);

  // Border colors
  static const Color borderColor = Color(0x40FFFFFF); // 25% opacity white
  static const Color checkboxBorder = accent;
}
